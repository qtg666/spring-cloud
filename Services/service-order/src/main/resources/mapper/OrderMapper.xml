<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qtg.mapper.OrderMapper">

    <insert id="insert">
        INSERT INTO orders (id, order_name, product_name, quantity)
        VALUES (#{id}, #{order_name}, #{product_name}, #{quantity})
    </insert>

</mapper>